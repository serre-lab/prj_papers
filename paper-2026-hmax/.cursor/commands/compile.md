Compile the LaTeX paper with tectonic.

Steps:
1) Run `make pdf` (or `./scripts/compile.sh` if Makefile is not present).
2) If it fails, read the error, make the minimal fix, and retry until it builds.
3) Summarize what changed.
